# NaniAI Chatbot - Python Dependencies
# Core runtime: Python 3.8+
# This project is intentionally lightweight with no heavy ML dependencies.

# Standard library modules used (no installation needed):
# - re         : Regular expression pattern matching
# - sys        : System-level operations
# - random     : Random response selection
# - string     : String utilities

# Optional: install only if your chatbot.py uses these
colorama==0.4.6       # Cross-platform colored terminal output
